import React from "react";

const SocialMedia = () => {
  return (
    <div>
      <button className="w-full btn btn-outline rounded-full mt-3">
        Continue with Google
      </button>
      <button className="w-full btn btn-outline rounded-full mt-3">
        Continue with FaceBook
      </button>
      <button className="w-full btn btn-outline rounded-full mt-3">
        Continue with GitHub
      </button>
    </div>
  );
};

export default SocialMedia;
